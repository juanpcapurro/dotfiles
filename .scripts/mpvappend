#!/bin/zsh

URL=${QUTE_URL}
SOCKET="${HOME}/.mpv/socket"

if [ -n "${VIDEO}" ] ; then
  lsof "${SOCKET}" || launch mpvs --idle --ytdl-format="bestvideo[height<=?1080]+bestaudio/best" 
else
  lsof "${SOCKET}" || launch mpvs --no-video --idle
fi

[ "$#" -eq 1 ] && URL="${1}"

while ! lsof "${SOCKET}"
do
  echo "waiting for mpvs to start"
  sleep 1
done

if [ -n "${URL}" ] ; then
  mpvctl add "${URL}" \
  && notify-send "Added ${URL} to playlist"\
  || notify-send "Couldnt add ${URL} to playlist"
else
  exit 1
fi

