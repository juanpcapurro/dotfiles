#!/bin/sh
xrandr \
  --output HDMI-1 --off\
  --output eDP-1 --pos 0x0 --mode 1366x768 --primary\
  && notify-send "On the move!" || notify-send "unable to undock :("
xset s 90
xset dpms 90 90 90
xset dpms force on
remapkeyboard
i3-msg restart
